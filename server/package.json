{
  "name": "dogum-haritasi-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Doğum Haritası uygulaması için Claude yorum vekili (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put ANTHROPIC_API_KEY",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.2026.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
